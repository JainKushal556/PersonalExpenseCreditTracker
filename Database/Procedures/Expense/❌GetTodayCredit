CREATE PROCEDURE spGetTodayCredit
AS
BEGIN
    SET NOCOUNT ON;

    SELECT 
        CreditID,
        UserID,
        CategoryID,
        Amount,
        Description,
        CreditAt
    FROM tblCredit
    WHERE CAST(CreditAt AS DATE) = CAST(GETDATE() AS DATE)
    ORDER BY CreditAt DESC;
END;
GO